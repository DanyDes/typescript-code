//tuples, an array strong typed
// takes the exact position and exact length
let userData: [string, number] = ['dany_des', 79554052];
//get the values by position, destructuring
let [username, telephone] = userData;
console.log(username, telephone);
